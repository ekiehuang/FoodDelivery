<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.southwind.repository.OrderRepository">

    <resultMap id="orderMap" type="Order">
        <id column="id" property="id"></id>
        <result column="date" property="date"></result>
        <result column="state" property="state"></result>
        <association property="menu" select="com.southwind.repository.MenuRepository.findById"
                     column="mid"></association>
    </resultMap>

    <insert id="save" parameterType="Order">
        insert into t_order(uid, mid, date, state)
        values (#{user.id}, #{menu.id}, #{date}, 0)
    </insert>

    <select id="findAll" resultMap="orderMap">
        select id, mid, date, state
        from t_order limit #{param1}, #{param2}
    </select>
</mapper>